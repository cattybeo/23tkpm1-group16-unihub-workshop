SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
STUDENT_EMAIL_DOMAIN=student.hcmus.edu.vn
STUDENT_DEFAULT_PASSWORD=123
DEFAULT_ACCOUNT_PASSWORD=123
CSV_IMPORT_DIR=../legacy-data

GEMINI_API_KEY=AIzaSy...
OPENROUTER_API_KEY=sk-or-v1-...

# Gmail SMTP
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Mock payment: set 1.0 để test circuit breaker mở
PAYMENT_MOCK_FAIL_RATE=0

PORT=3000
NODE_ENV=development

# Comma-separated origins allowed to send credentials (cookies).
# Wildcard cannot be used with credentials:true.
ALLOWED_ORIGINS=http://localhost:5173

# Optional. Set only if FE and BE are on different subdomains of the same site.
# COOKIE_DOMAIN=.unihub.example.com
